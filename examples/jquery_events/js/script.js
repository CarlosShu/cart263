// // Only changes one header to red. Stops on first click.
// $(`.header`).on(`click`, function (event) {
//   $(this).css(`color`, `red`);
//   $(`.header`).off(`click`);
// });

// // Adds only on first click.
// $(`section`).one("click", function (event) {
//   $(this).append(`<p>This will be addeded on the first click.</p>`);
// });

// // Adds on every click.
// $(`section`).on(`click`, function (Event) {
//   $(this).append(`<p>This will be addeded on every click.</p>`);
// });

// // Remvoes anything with header class on click.
// $(`.header`).on(`click`, function (Event) {
//   $(this).remove();
// });
